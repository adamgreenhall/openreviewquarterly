-@title='ORQ Contributors'
-@bodyid='authors_list'
#all_authors
  - @authors.enum_slice(3).to_a.each do |row|
    .row
      -row.each do |author|
        .contributor.span3
          .pic=link_to author_photo(author), author.url
          .name=link_to_author(author)